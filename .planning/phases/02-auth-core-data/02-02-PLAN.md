---
phase: 02-auth-core-data
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - package.json
  - package-lock.json
  - lib/pocketbase-server.ts
  - lib/pocketbase-browser.ts
  - lib/pocketbase.ts
  - components/ui/button.tsx
  - components/ui/input.tsx
  - components/ui/label.tsx
  - components/ui/card.tsx
  - components/ui/form.tsx
  - components/ui/select.tsx
  - components/ui/dialog.tsx
  - components/ui/dropdown-menu.tsx
  - components/ui/tabs.tsx
  - components/ui/separator.tsx
  - components/ui/sonner.tsx
  - app/globals.css
  - components.json
  - tsconfig.json
  - tests/unit/pocketbase-server.test.ts
autonomous: true
requirements:
  - AUTH-02

must_haves:
  truths:
    - "Calling createServerClient() inside a Server Component returns a PB client whose authStore.token matches the pb_auth cookie value"
    - "Calling createServerClient() when no pb_auth cookie is present returns a PB client with authStore.isValid === false"
    - "getBrowserClient() returns a singleton PB client pointing at window.location.origin"
    - "app/globals.css defines --primary via #D4A574-equivalent HSL mapped through @theme inline"
    - "All 11 shadcn components from D-14 exist under components/ui/ and import from the shadcn-generated locations"
    - "react-hook-form, @hookform/resolvers, @dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities, sonner, lucide-react, date-fns-tz are installed at exact pinned versions"
  artifacts:
    - path: "lib/pocketbase-server.ts"
      provides: "createServerClient() — per-request PB client hydrated from pb_auth cookie"
      contains: "createServerClient"
    - path: "lib/pocketbase-browser.ts"
      provides: "getBrowserClient() — browser singleton PB client pointing at origin"
      contains: "getBrowserClient"
    - path: "app/globals.css"
      provides: "Tailwind 4 CSS-first theme with #D4A574 primary accent"
      contains: "@theme inline"
    - path: "components/ui/button.tsx"
      provides: "shadcn button component (Button export)"
      contains: "Button"
    - path: "tests/unit/pocketbase-server.test.ts"
      provides: "Vitest unit test mocking next/headers cookies() and asserting pb_auth load"
      contains: "createServerClient"
    - path: "components.json"
      provides: "shadcn CLI config (style, base color, CSS variables)"
      contains: "new-york"
  key_links:
    - from: "lib/pocketbase-server.ts"
      to: "next/headers cookies()"
      via: "await cookies() → cookieStore.get('pb_auth') → pb.authStore.loadFromCookie"
      pattern: "loadFromCookie"
    - from: "lib/pocketbase-browser.ts"
      to: "window.location.origin"
      via: "new PocketBase(window.location.origin) singleton"
      pattern: "window.location.origin"
    - from: "app/globals.css"
      to: "shadcn components"
      via: "--primary / --background / --foreground CSS variables"
      pattern: "--primary"
    - from: "tests/unit/pocketbase-server.test.ts"
      to: "lib/pocketbase-server.ts"
      via: "vi.mock('next/headers') + import createServerClient"
      pattern: "vi\\.mock\\(['\"]next/headers"
---

<objective>
Install the Phase 2 frontend runtime — shadcn/ui components and supporting libs (react-hook-form, @dnd-kit, date-fns-tz, sonner) — and build the SSR cookie bridge that lets Server Components, Server Actions, and Route Handlers read/write the `pb_auth` cookie (D-03). Also wire the Tailwind 4 warm-accent theme per D-18/§19 so every downstream plan inherits the design tokens.

Purpose: Every UI plan downstream (02-03 auth pages, 02-04 homes/areas CRUD, 02-05 tasks CRUD) imports `createServerClient` from `lib/pocketbase-server.ts` and renders shadcn `<Button>`, `<Input>`, `<Form>`, `<DropdownMenu>`, etc. Without this plan the downstream plans cannot start. The cookie bridge is the D-03 linchpin — any bug here is an auth bug affecting AUTH-02 session persistence.

Output: Installed deps, shadcn-scaffolded `components/ui/*`, themed `app/globals.css`, two new client factory files (`lib/pocketbase-server.ts`, `lib/pocketbase-browser.ts`), and a unit test proving the server factory reads the cookie correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-auth-core-data/02-CONTEXT.md
@.planning/phases/02-auth-core-data/02-RESEARCH.md

<!-- RESEARCH.md §Standard Stack (lines 111-160) — exact version pins + install commands. -->
<!-- RESEARCH.md §Pattern: SSR Cookie Bridge (lines 583-675) — verbatim server + browser factory code. -->
<!-- RESEARCH.md §Pitfall 9 (lines 1320-1357) — Tailwind 4 @theme inline pattern + HSL tokens. -->
<!-- RESEARCH.md §Pitfall 5 (lines 1288-1294) — browser authStore is display-only cache (HttpOnly cookie invisible to JS). -->
<!-- RESEARCH.md §Assumptions A6 — "stone" is shadcn's warmest base. -->

@lib/pocketbase.ts
@app/globals.css
@app/layout.tsx
@package.json
@tsconfig.json
@vitest.config.ts

<interfaces>
<!-- Phase 1 Pattern established in lib/pocketbase.ts: `createClient()` branches on typeof window. -->
<!-- This plan SUPERSEDES the single factory with two specialized factories per RESEARCH.md §Architectural Responsibility Map: -->
<!--   - lib/pocketbase-server.ts → createServerClient(): Promise<PocketBase> — hydrated from cookie -->
<!--   - lib/pocketbase-browser.ts → getBrowserClient(): PocketBase — singleton, throws in SSR -->
<!-- Keep lib/pocketbase.ts as a thin re-export shim so Phase 1 test (tests/unit/pocketbase.test.ts) doesn't regress. -->

<!-- Next 16 cookies() API — CRITICAL -->
import { cookies } from 'next/headers';
// cookies() is ASYNC since Next 15 RC — must await.
const cookieStore = await cookies();
const value = cookieStore.get('pb_auth')?.value;

<!-- PocketBase JS SDK (v0.26.8) authStore API -->
pb.authStore.loadFromCookie(cookieHeader: string): void
pb.authStore.exportToCookie(options?: CookieOptions, key?: string): string  // returns full Set-Cookie header string
pb.authStore.isValid: boolean
pb.authStore.record: RecordModel | null
pb.authStore.token: string
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install deps + run shadcn init + scaffold theme + keep Phase 1 test intact</name>
  <read_first>
    - package.json (exact-pinned Phase 1 deps — preserve all)
    - .planning/phases/02-auth-core-data/02-RESEARCH.md §Standard Stack — Installation Commands (lines 139-158)
    - .planning/phases/02-auth-core-data/02-RESEARCH.md §Pitfall 9 — Tailwind 4 @theme inline + HSL tokens (lines 1320-1357)
    - app/globals.css (currently just `@import "tailwindcss"`)
    - app/layout.tsx (existing RootLayout — shadcn init may request `<Toaster />` placement)
  </read_first>
  <files>
    package.json,
    package-lock.json,
    components/ui/button.tsx,
    components/ui/input.tsx,
    components/ui/label.tsx,
    components/ui/card.tsx,
    components/ui/form.tsx,
    components/ui/select.tsx,
    components/ui/dialog.tsx,
    components/ui/dropdown-menu.tsx,
    components/ui/tabs.tsx,
    components/ui/separator.tsx,
    components/ui/sonner.tsx,
    app/globals.css,
    components.json,
    tsconfig.json
  </files>
  <action>
    Follow RESEARCH §Standard Stack installation commands exactly. Use exact pins (no semver carets) per Phase 1's established "exact-pinned" pattern.

    1. **Install runtime deps** — append to `package.json` dependencies:
       ```
       "react-hook-form": "7.73.1",
       "@hookform/resolvers": "5.2.2",
       "@dnd-kit/core": "6.3.1",
       "@dnd-kit/sortable": "10.0.0",
       "@dnd-kit/utilities": "3.2.2",
       "sonner": "2.0.7",
       "lucide-react": "1.8.0",
       "date-fns-tz": "3.2.0"
       ```
       Optionally bump `zod` from `4.1.0` → `4.3.6` per RESEARCH — acceptable either way; if bumping triggers any test failure, keep 4.1.0 and note in summary.

       Run `npm install` and commit the resulting `package-lock.json`. If any version is unpublished (see Phase 1 pattern — registry deltas), fall back to latest published in the same major line and document the delta in the summary like Phase 1 did.

    2. **Run `npx shadcn@4.3.1 init` (non-interactive mode where possible)** — this creates `components.json`, edits `app/globals.css`, and may edit `tsconfig.json` to add `@/*` path aliases (already present from Phase 1 — shadcn should leave them alone).

       Choices (answer these prompts; if non-interactive flags are available on shadcn@4.3.1, use them instead):
       - Style: **new-york**
       - Base color: **stone** (warmest per RESEARCH A6)
       - CSS variables: **yes**
       - Install lucide-react: **skip** (already pinned in step 1)

       Accept shadcn's writes to `app/globals.css` and `components.json`.

    3. **After init, manually override `app/globals.css`** to use the D-18 warm accent (#D4A574 as HSL `30 45% 65%`) per RESEARCH §Pitfall 9 verbatim (lines 1325-1355). Specifically ensure:
       - `:root` block contains `--primary: hsl(30 45% 65%);` (terracotta-sand approximation)
       - `:root` block contains `--radius: 0.75rem;` (rounded-lg+ per SPEC §19)
       - `@theme inline { ... }` block maps `--color-primary: var(--primary)`, `--color-background`, `--color-foreground`, `--color-card`, `--color-muted`, `--color-border`, `--radius-lg: var(--radius)`.
       - Keep any other shadcn defaults (sidebar tokens, chart tokens) that shadcn emits — don't delete them; just ensure the primary/radius overrides match SPEC §19.

    4. **Install shadcn components** — run:
       ```
       npx shadcn@4.3.1 add button input label card form select dialog dropdown-menu tabs separator sonner
       ```
       This creates 11 files under `components/ui/`. Each is a vendored component (not a runtime dep). shadcn also ensures `class-variance-authority`, `clsx`, `tailwind-merge` are in devDependencies if needed — accept those additions.

    5. **Verify `tsconfig.json`** — shadcn init may (re)write `paths` or `baseUrl`. Phase 1 already has `"@/*": ["./*"]` configured. If shadcn breaks anything, restore from git HEAD and manually ensure:
       - `"baseUrl": "."`
       - `"paths": { "@/*": ["./*"] }`
       - `"ignoreDeprecations": "6.0"` (Phase 1 decision for TS 6)
       These are the Phase 1-established values; shadcn should leave them.

    6. **Add `<Toaster />` to root layout** — edit `app/layout.tsx` and add `import { Toaster } from '@/components/ui/sonner';` plus `<Toaster />` just inside the `<body>` (after `{children}`). This wires `sonner` for toast use in downstream plans.

    Critical do-NOT list (per RESEARCH Pitfall 9 and Phase 1 patterns):
    - Do NOT create a `tailwind.config.js` — Tailwind 4 is CSS-first.
    - Do NOT use semver carets for new deps — pin exact versions (Phase 1 established pattern per 01-01-SUMMARY.md "Exact-pin all deps").
    - Do NOT delete `app/api/health/route.ts` or modify `lib/pocketbase.ts` in this task (next task handles the cookie bridge).
    - Do NOT run `shadcn init` with `--force` — if it refuses because components.json already exists, inspect and overwrite manually.
  </action>
  <behavior>
    - Given a clean `npm install` → all new deps resolve at their exact pins; lockfile updates cleanly.
    - Given `app/globals.css` is loaded by the build → compiled CSS contains a `--color-primary` custom property pointing to an hsl(30 45% 65%) value.
    - Given `components/ui/button.tsx` exists → importing `{ Button }` from `@/components/ui/button` in a test file resolves and renders without error.
    - Given `npm run build` → Next produces a `.next/standalone/server.js` (the Phase 1 artifact still works).
  </behavior>
  <verify>
    <automated>
      # Deps pinned
      grep -q '"react-hook-form": "7.73.1"' package.json && \
      grep -q '"@hookform/resolvers": "5.2.2"' package.json && \
      grep -q '"@dnd-kit/core": "6.3.1"' package.json && \
      grep -q '"@dnd-kit/sortable": "10.0.0"' package.json && \
      grep -q '"@dnd-kit/utilities": "3.2.2"' package.json && \
      grep -q '"sonner": "2.0.7"' package.json && \
      grep -q '"lucide-react": "1.8.0"' package.json && \
      grep -q '"date-fns-tz": "3.2.0"' package.json && \
      # shadcn init outputs
      test -f components.json && \
      grep -q '"style": "new-york"' components.json && \
      # 11 shadcn components landed
      test -f components/ui/button.tsx && \
      test -f components/ui/input.tsx && \
      test -f components/ui/label.tsx && \
      test -f components/ui/card.tsx && \
      test -f components/ui/form.tsx && \
      test -f components/ui/select.tsx && \
      test -f components/ui/dialog.tsx && \
      test -f components/ui/dropdown-menu.tsx && \
      test -f components/ui/tabs.tsx && \
      test -f components/ui/separator.tsx && \
      test -f components/ui/sonner.tsx && \
      # Tailwind 4 theme
      grep -q '@theme inline' app/globals.css && \
      grep -q -- '--primary' app/globals.css && \
      grep -qE -- '--radius' app/globals.css && \
      # Toaster wired
      grep -q 'Toaster' app/layout.tsx && \
      # Phase 1 guarantees still hold
      npm run lint && \
      npm run typecheck && \
      npm test && \
      npm run build
    </automated>
  </verify>
  <done>
    All 8 new runtime deps pinned. 11 shadcn components exist. `components.json` has style=new-york. `app/globals.css` has @theme inline + --primary + --radius. `<Toaster />` imported in `app/layout.tsx`. Full lint + typecheck + test + build all pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write SSR cookie bridge (lib/pocketbase-server.ts + lib/pocketbase-browser.ts) with unit tests</name>
  <read_first>
    - .planning/phases/02-auth-core-data/02-RESEARCH.md §Pattern: SSR Cookie Bridge (lines 583-675) — full verbatim code for both files
    - .planning/phases/02-auth-core-data/02-RESEARCH.md §Pitfall 5 (lines 1288-1294) — browser authStore is display-only; HttpOnly cookie invisible to JS
    - lib/pocketbase.ts (Phase 1 factory — keep as re-export shim so tests/unit/pocketbase.test.ts doesn't regress)
    - tests/unit/pocketbase.test.ts (Phase 1 test — must still pass after today's changes)
    - tests/setup.ts
    - vitest.config.ts
  </read_first>
  <files>
    lib/pocketbase-server.ts,
    lib/pocketbase-browser.ts,
    lib/pocketbase.ts,
    tests/unit/pocketbase-server.test.ts
  </files>
  <action>
    TDD: write the unit test first (RED commit), then implement (GREEN commit). Follow Phase 1's plan-level TDD pattern.

    **RED step — write failing test.**

    Create `tests/unit/pocketbase-server.test.ts` (jsdom env is fine — we mock `next/headers` so no real Next runtime needed). Structure:

    ```typescript
    import { describe, test, expect, vi, beforeEach } from 'vitest';

    // Mock next/headers BEFORE importing the module under test.
    const mockGet = vi.fn();
    vi.mock('next/headers', () => ({
      cookies: vi.fn(async () => ({ get: mockGet })),
    }));

    describe('createServerClient', () => {
      beforeEach(() => {
        mockGet.mockReset();
      });

      test('returns PB client pointed at 127.0.0.1:8090', async () => {
        mockGet.mockReturnValue(undefined);
        const { createServerClient } = await import('@/lib/pocketbase-server');
        const pb = await createServerClient();
        expect(pb.baseURL).toBe('http://127.0.0.1:8090');
      });

      test('hydrates authStore when pb_auth cookie is present', async () => {
        // A minimally valid PB cookie value is a URL-encoded JSON string.
        // Use a shape the SDK accepts: {"token":"t","model":{"id":"u1"}}.
        const rawJson = '{"token":"testtoken","model":{"id":"u1","collectionName":"users"}}';
        const encoded = encodeURIComponent(rawJson);
        mockGet.mockReturnValue({ value: encoded, name: 'pb_auth' });

        const { createServerClient } = await import('@/lib/pocketbase-server');
        const pb = await createServerClient();
        expect(pb.authStore.token).toBe('testtoken');
        expect(pb.authStore.record?.id).toBe('u1');
      });

      test('authStore is empty when no cookie', async () => {
        mockGet.mockReturnValue(undefined);
        const { createServerClient } = await import('@/lib/pocketbase-server');
        const pb = await createServerClient();
        expect(pb.authStore.token).toBe('');
        expect(pb.authStore.record).toBeNull();
      });

      test('each call returns a fresh client (no cross-request auth leakage)', async () => {
        mockGet.mockReturnValue(undefined);
        const { createServerClient } = await import('@/lib/pocketbase-server');
        const a = await createServerClient();
        const b = await createServerClient();
        expect(a).not.toBe(b);
      });
    });
    ```

    Run `npm test` → test fails because `lib/pocketbase-server.ts` doesn't exist. Commit as RED: `test(02-02): add failing tests for SSR cookie bridge`.

    **GREEN step — implement both factories.**

    1. **`lib/pocketbase-server.ts`** — use the verbatim code from RESEARCH.md §Pattern: SSR Cookie Bridge lines 593-639 (both `createServerClient` and `createServerClientWithRefresh`). The server client:
       - Imports `PocketBase` from `'pocketbase'` and `{ cookies }` from `'next/headers'`
       - Returns a `Promise<PocketBase>`
       - Instantiates `new PocketBase('http://127.0.0.1:8090')` (loopback — matches Phase 1 D-03)
       - `await cookies()` → get `pb_auth` → if present, `pb.authStore.loadFromCookie(\`pb_auth=${value}\`)`
       - Never caches or module-level-instantiates
       - Also exports `createServerClientWithRefresh()` which additionally calls `authRefresh()` when authStore.isValid (wraps in try/catch and calls `pb.authStore.clear()` on failure) — for future use in layout guards
       - Add a JSDoc block at the top of `createServerClient` citing D-03 and the per-request requirement.

    2. **`lib/pocketbase-browser.ts`** — use RESEARCH.md §Pattern: SSR Cookie Bridge lines 643-670 verbatim. The browser client:
       - First line: `'use client';`
       - Module-scoped `let pbInstance: PocketBase | null = null;`
       - Exports `getBrowserClient(): PocketBase` returning a singleton pointed at `window.location.origin`
       - Throws in SSR context (`typeof window === 'undefined'`) so accidental server calls fail fast
       - Add JSDoc explaining Pitfall 5 (HttpOnly → authStore is display-only cache; cookie attached automatically on same-origin fetches; do NOT try to read token via `document.cookie`)

    3. **`lib/pocketbase.ts` — reduce to a re-export shim** so Phase 1's `tests/unit/pocketbase.test.ts` still passes without modification. Replace contents with:
       ```typescript
       /**
        * Phase 1 compatibility shim.
        *
        * This factory exists so tests/unit/pocketbase.test.ts (Phase 1) keeps passing.
        * NEW CODE MUST use:
        *   - lib/pocketbase-server.ts → createServerClient() for Server Components, Route Handlers, Server Actions
        *   - lib/pocketbase-browser.ts → getBrowserClient() for Client Components
        * See D-03 and RESEARCH.md §Pattern: SSR Cookie Bridge.
        */
       import PocketBase from 'pocketbase';

       export function createClient(): PocketBase {
         if (typeof window === 'undefined') {
           return new PocketBase('http://127.0.0.1:8090');
         }
         return new PocketBase(window.location.origin);
       }
       ```
       This preserves the Phase 1 API (both branches) — tests/unit/pocketbase.test.ts still asserts `createClient()` returns loopback in server vs origin in browser.

    Run `npm test` → all tests pass (new + Phase 1). Commit as GREEN: `feat(02-02): SSR cookie bridge for Server Components + singleton browser client`.

    **Do-NOT list:**
    - Do NOT alter the Phase 1 `lib/pocketbase.ts` API shape (the `createClient` export) — the test imports that name.
    - Do NOT import from `next/headers` inside `lib/pocketbase-browser.ts` — that package is server-only.
    - Do NOT module-level instantiate the server client — would leak auth between concurrent requests.
    - Do NOT attempt to read `document.cookie` in the browser file (HttpOnly; see Pitfall 5).
  </action>
  <behavior>
    - Test 1: `createServerClient()` always returns a client with `baseURL === 'http://127.0.0.1:8090'` (loopback).
    - Test 2: When `next/headers` cookies().get('pb_auth') returns `{value: urlEncoded(validJson)}`, the returned client's `authStore.token === 'testtoken'` and `authStore.record.id === 'u1'`.
    - Test 3: When cookie is absent, `authStore.token === ''` and `authStore.record === null`.
    - Test 4: Two consecutive `createServerClient()` calls return distinct instances (no module-level cache).
  </behavior>
  <verify>
    <automated>
      test -f lib/pocketbase-server.ts && \
      grep -q 'createServerClient' lib/pocketbase-server.ts && \
      grep -q "from 'next/headers'" lib/pocketbase-server.ts && \
      grep -q 'loadFromCookie' lib/pocketbase-server.ts && \
      grep -q 'http://127.0.0.1:8090' lib/pocketbase-server.ts && \
      ! grep -q 'let pb' lib/pocketbase-server.ts && \
      test -f lib/pocketbase-browser.ts && \
      grep -q "'use client'" lib/pocketbase-browser.ts && \
      grep -q 'getBrowserClient' lib/pocketbase-browser.ts && \
      grep -q 'window.location.origin' lib/pocketbase-browser.ts && \
      test -f lib/pocketbase.ts && \
      grep -q 'export function createClient' lib/pocketbase.ts && \
      test -f tests/unit/pocketbase-server.test.ts && \
      grep -q "vi.mock\\('next/headers'" tests/unit/pocketbase-server.test.ts && \
      npm run lint && \
      npm run typecheck && \
      npm test
    </automated>
  </verify>
  <done>
    Both factory files exist with the verbatim research code. Phase 1 shim keeps `createClient` export. Four unit tests pass + Phase 1 tests still pass. Lint + typecheck clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser cookie jar → Next.js server action / RSC | pb_auth cookie crosses here on every request; Next framework owns Set-Cookie header |
| lib/pocketbase-server.ts → next/headers cookies() | Server-only API surface; must not be reachable from Client Components |
| Client Component → getBrowserClient() | Returns singleton; must not expose the authStore token directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Info Disclosure | Module-level PB instance leaks auth between concurrent requests | mitigate | `createServerClient()` instantiates a fresh `new PocketBase(...)` per call — no module-level state (RESEARCH §Pattern: SSR Cookie Bridge "CRITICAL" note). Unit test 4 asserts two calls return distinct instances. |
| T-02-02-02 | Tampering | Server code imports `lib/pocketbase-browser.ts` → runtime error OR accidental token exposure | mitigate | `lib/pocketbase-browser.ts` starts with `'use client'` directive; `getBrowserClient()` throws when `typeof window === 'undefined'` (guard inside the function). |
| T-02-02-03 | Info Disclosure | XSS reads `pb_auth` via `document.cookie` | mitigate | pb_auth is HttpOnly (set in 02-03 server action); browser SDK never attempts to read it (Pitfall 5 documented in lib/pocketbase-browser.ts JSDoc). |
| T-02-02-04 | Tampering | shadcn init writes unexpected config values to tsconfig.json → Phase 1 `@/*` alias breaks | accept | Task 1 action verifies `@/*` alias + ignoreDeprecations survive shadcn init; if they're broken, restore from git and adjust. Phase 1's `tests/unit/pocketbase.test.ts` uses `@/lib/pocketbase` import — acts as a regression sentinel. |
| T-02-02-05 | Info Disclosure | Sonner toast accidentally renders a PB error string containing PII (email / token) | mitigate | Not in this plan's scope to enforce — downstream server actions (02-03) return sanitized `{formError}` strings per RESEARCH §Security Domain V7. |
</threat_model>

<verification>
- `npm run lint && npm run typecheck` green.
- `npm test` green (4 new tests + Phase 1's health/pocketbase/smoke tests still pass).
- `npm run build` green — standalone output still builds (Phase 1 artifact contract preserved).
- Manual smoke (optional): `import { Button } from '@/components/ui/button'` resolves in a throwaway page; `<Button variant="default">hi</Button>` renders with the warm #D4A574 primary color.
</verification>

<success_criteria>
1. `react-hook-form@7.73.1`, `@hookform/resolvers@5.2.2`, `@dnd-kit/core@6.3.1`, `@dnd-kit/sortable@10.0.0`, `@dnd-kit/utilities@3.2.2`, `sonner@2.0.7`, `lucide-react@1.8.0`, `date-fns-tz@3.2.0` all installed at exact pins.
2. All 11 D-14 shadcn components scaffolded under `components/ui/`: button, input, label, card, form, select, dialog, dropdown-menu, tabs, separator, sonner.
3. `app/globals.css` uses Tailwind 4 `@theme inline` with `--primary` set to `#D4A574`-equivalent HSL and `--radius` at 0.75rem.
4. `lib/pocketbase-server.ts` exports `createServerClient()` and `createServerClientWithRefresh()` — per-request, cookie-hydrated, loopback-scoped.
5. `lib/pocketbase-browser.ts` exports `getBrowserClient()` — singleton, origin-scoped, fails fast on SSR misuse.
6. `lib/pocketbase.ts` reduced to a back-compat shim exposing `createClient()` so Phase 1 tests stay green (AUTH-02 session persistence plumbing is correctly factored without regressing INFR-05 health check).
7. `tests/unit/pocketbase-server.test.ts` covers: baseURL, cookie hydration success, no-cookie empty state, and no-cache-per-call (4 tests).
8. Lint, typecheck, full test suite, and build all green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-core-data/02-02-SUMMARY.md` covering:
- Installed versions (actual vs planned — document deltas per Phase 1 pattern).
- shadcn init output (base color, style, any config.json surprises).
- Tailwind 4 theme colors used and how they render (mention #D4A574 HSL approximation).
- Confirmation that Phase 1 `lib/pocketbase.ts` shim preserves `createClient` API and that `tests/unit/pocketbase.test.ts` still passes (regression sentinel).
- Any ESLint / TypeScript rule adjustments triggered by the new libraries.
- Deviations from RESEARCH (e.g., if shadcn@4.3.1 prompts unexpected config).
</output>
