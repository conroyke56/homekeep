# VPS operating manual — the-kizz.com multi-project host

**Install location:** `/opt/vps/CLAUDE.md` (mode 644, root-owned).
**Symlink target:** each service user's `~/.claude/CLAUDE.md`.

> This file is loaded by every Claude Code session that runs under a
> user whose `~/.claude/CLAUDE.md` symlinks here. It carries VPS-wide
> operating context so one edit propagates to every project.

---

## Host

- **Hetzner VPS** (46.62.151.57, IPv6 `2a01:4f9:c013:3b16::1`), Ubuntu 24.04
- **UFW:** 22, 80, 443 always open; per-project 127.0.0.1-bound ports never public
- **Docker:** 29.x rootful, userns-remap enabled — **host bind-mounts
  break** for containers that run as non-root; use **named volumes**
  or `tmpfs` for container-writable paths
- **Projects live under:**
  - `/opt/vps/<thing>/` — VPS-wide shared infrastructure (revproxy,
    vhosts, reports, PORTS.md ledger, bin helpers). Root-owned,
    ACL'd for project users as needed.
  - `/home/sprout/<stuff>/` — sprout's personal dev work (Sprout M0
    etc.). Peer tenant; no special ops status as of Phase 34
    (2026-04-24).
  - `/srv/<project>/` — dedicated service users' home (e.g.
    `/srv/homekeep` for the `homekeep` user)
  - `/opt/<project>/` — legacy path for earlier projects (aom-wiki)
- Every service user is in the `docker` group → effectively root at the
  daemon level. Treat per-user isolation as defense-in-depth only.

### Shared infrastructure locations (root-owned, project users ACL'd)

| Path | Owner / perms | Purpose |
|---|---|---|
| `/opt/vps/CLAUDE.md` | root:root 644 | this file; imported by each service user |
| `/opt/vps/PORTS.md` | root:root 644 | port allocation ledger (`127.0.0.1:3000-3999`) |
| `/opt/vps/revproxy/` | root:root | shared reverse proxy (Phase 34, 2026-04-24) |
| `/opt/vps/vhosts/` | root:secrets 775 + default ACL `rwx` for `homekeep`,`claude-dev`,`sprout` | per-project Caddy vhost snippets; project users drop their own |
| `/etc/secrets/` | root:secrets | shared creds (`godaddy.creds`, future: `ntfy.creds`, etc.) |
| `/etc/gitignore-vps-baseline` | root:root 644 | system-wide git `core.excludesFile` |
| `/etc/git-template/` | root:root | new-repo template (pre-commit hook runs gitleaks) |
| `/opt/vps/bin/` | root:root | shared helper scripts (`claude-secret-guard`, followup-*.sh) |
| `/opt/vps/reports/` | root:root 755 | Claude-session execution reports (phase completions, root-sync runs) |
| `/opt/vps/FOLLOWUP-*.md` | root:root 644 | surfaced by systemd timers for deferred work |
| `/opt/vps/SECRET-LEAK-RUNBOOK.md` | root:root 644 | rotation-first playbook if a credential leaks |

## Multi-project serving pattern

### Subdomain convention on `the-kizz.com`

- `<project>.the-kizz.com` — prod
- `<project>.dev.the-kizz.com` — staging
- `<project>.demo.the-kizz.com` — public demo / tmpfs ephemeral

Admin UIs live **behind the same origin** as `/admin` path-prefix with
auth middleware, NOT as a separate subdomain. Keeps cookies same-origin
and avoids recon surface.

### Domain scope — read before picking a subdomain

- **`the-kizz.com`** = this VPS. Public / GitHub / multi-project work
  goes here. Wildcard `*.the-kizz.com → 46.62.151.57`.
- **`kizz.space`** = the operator's private home-network server
  (audiobooks, booking, budget, calibre, plex, sonarr, etc.). Wildcard
  `*.kizz.space → 144.6.229.170`. **Do not provision VPS services on
  `kizz.space`** — pick `the-kizz.com` instead.
- Legacy exception: `sprout-m0.kizz.space` still points at this VPS
  via an explicit A record; it's the only `kizz.space` subdomain
  served from here and will migrate to `sprout-m0.the-kizz.com`
  eventually.

### DNS

- Wildcard A record `*.the-kizz.com → 46.62.151.57` is live
  (created 2026-04-24). **Every new VPS subdomain resolves
  automatically — no GoDaddy API call needed per project.**
- Apex `@` / special records (MX, TXT for mail, etc.) still need
  explicit entries.
- GoDaddy creds: `/home/sprout/.config/godaddy/credentials` (mode 600,
  sprout-only) OR `/etc/secrets/godaddy.creds` (once centralized, with
  POSIX ACLs per consuming project).

### TLS

- `revproxy-caddy-1` (Caddy 2.x, `--network host`, compose project
  `revproxy`) terminates TLS on 80/443.
- **Preferred:** wildcard cert for `*.the-kizz.com` via DNS-01 using
  the `caddy-dns/godaddy` plugin. One cert covers every subdomain,
  past and future.
- **Fallback (current):** per-subdomain HTTP-01 challenge via port 80.
  Works but slower to provision new subdomains.
- Caddyfile at `/opt/vps/revproxy/Caddyfile` imports vhosts from
  `/opt/vps/vhosts/*.caddy`. Project users drop their own project's
  vhost directly into `/opt/vps/vhosts/` — ACL'd for write (Phase 34,
  2026-04-24).

### Reverse proxy → project

- Every project binds its container(s) to **`127.0.0.1:<port>`** on the
  host (never `0.0.0.0`). Ports allocated from
  `/opt/vps/PORTS.md` to avoid collisions.
- Shared Caddy proxies `<project>.the-kizz.com` → `127.0.0.1:<port>`
  via a vhost snippet the project ships in its repo.
- Per-project Docker Compose uses `-p <project-name>` for
  namespace isolation.

## Secrets

### Per-project `/srv/<project>/.env` (mode 600)

Owned by that project's service user. Should contain:
- `GITHUB_PAT` — **fine-grained, scoped to this repo only**
  (not a shared classic PAT). Rotate in the GitHub UI when needed.
- Per-project secrets (DB, SMTP, API keys) specific to this project.

### Shared creds at `/etc/secrets/` (once centralized)

Files like `godaddy.creds`, `ntfy.creds`, etc. owned `root:secrets`,
mode `640`. Per-project ACL grants: `setfacl -m u:<project>:r
/etc/secrets/<file>`.

**Do NOT duplicate shared creds into each service user's home** —
rotation becomes a multi-file ritual and drift is inevitable.

## Secrets hygiene — mandatory for every Claude Code session

Operator-enforced rules. These exist because AI-assisted coding
roughly **doubles** the base rate of secret leaks (measured: ~3.2%
of Claude-assisted commits vs ~1.5% human-only). Most leaks cluster
into a handful of failure modes listed below. The VPS has multiple
enforcement layers (Claude PreToolUse hook + git pre-commit hook +
GitHub push protection + gitleaks CI action) but operator discipline
is the first and most important layer.

### Rules (most-violated first)

1. **Never paste a real credential value into any file under a git
   working tree.** Use placeholders: `<GODADDY_API_KEY>`,
   `<GITHUB_PAT>`, `<STRIPE_SK>`. Real values go in
   `/etc/secrets/<name>` or `/srv/<project>/.env` only; read at
   runtime.
2. **Never put a real credential in a Markdown file.** Not in
   examples, not in runbooks, not in troubleshooting docs. This is
   the #1 vibe-coding leak vector because `.gitignore` does nothing
   for `.md` files. The pre-tool hook hard-blocks assignment-shaped
   lines in `.md`.
3. **Never construct `|| "fallback"` patterns for secrets.** If the
   env var is missing, throw. A working hardcoded fallback is a leak
   waiting to be committed.
4. **Never `git add .` or `git add -A`.** Always add named paths —
   prevents scratch files, session transcripts, `.env.local`,
   `dev.sqlite3`, Postman exports, `.har` files sneaking in.
5. **Never commit `*.db`, `*.sqlite`, `*.sqlite3`, `*.har`, `*.pcap`
   without explicit operator confirmation** — they routinely contain
   session tokens, hashed passwords, OAuth refresh tokens.
6. **MCP config files (`mcp.json`, `.mcp/*.json`) are credentials.**
   Never commit with real tokens. Use `env:` substitution or a
   per-machine config at `~/.config/claude/mcp.json`.
7. **Before any `git push`, read the staged diff back.** Mentally
   scan for long strings of entropy in assignment or URL-path
   contexts.
8. **If you paste a real credential into chat to debug something,
   that session transcript is now contaminated.** Do not commit
   anything under `.claude/sessions/` (already globally ignored).
9. **If you believe a secret has been leaked, stop and escalate.**
   Don't try to "fix it with a force-push" — see the rotation
   runbook at `/opt/vps/SECRET-LEAK-RUNBOOK.md`. Rotation is
   non-negotiable.

### Enforcement layers installed on this VPS (2026-04-24)

- **Claude Code PreToolUse hook** at `/opt/vps/bin/claude-secret-guard`
  — runs `gitleaks stdin --redact` on every `Write`/`Edit`/`MultiEdit`
  before the file hits disk. Wired into `/etc/skel/.claude/settings.json`
  so new users inherit it; retrofitted into sprout, homekeep, claude-dev.
- **System-wide git pre-commit hook** at `/etc/git-template/hooks/pre-commit`
  — runs `gitleaks protect --staged` on every commit in every new
  repo (via `git config --system init.templateDir`). Retrofitted
  into `/srv/homekeep/.git/hooks/pre-commit` and
  `/root/projects/sprout/.git/hooks/pre-commit`.
- **System-wide `.gitignore` baseline** at `/etc/gitignore-vps-baseline`
  — covers `.env`, `*.pem`, `.claude/sessions/`, `CLAUDE.local.md`,
  etc. Additive to each repo's own `.gitignore`.
- **GitHub repo-level push protection + secret scanning** (enabled
  per `the-kizz` org repo — check with `gh api repos/the-kizz/<repo>`).
- **GitHub Actions `gitleaks-action` on PRs** for active repos
  (homekeep, sprout) — see `.github/workflows/gitleaks.yml` in each.

### Bypassing (and when it's legitimate)

- **Single-line false positive in code:** add `# gitleaks:allow` at
  the end of the line. Gitleaks honors it natively.
- **Single-line false positive in Markdown:** add
  `<!-- gitleaks:allow -->` on the same line.
- **One-off commit bypass (discouraged):** `git commit --no-verify`.
  Server-side push protection + the gitleaks CI action still fire.
- **Allowlist a finding permanently:** run `gitleaks protect --staged -v`
  locally, copy the `Fingerprint:` value into `.gitleaksignore` at
  the repo root, commit that file.

## Per-project onboarding checklist

Run as root. Assumes an **empty** `<project>` label, e.g. `wiki`,
`notes`, etc.

```bash
PROJECT=<project>

# 1. Service user + home
#    adduser populates ~/.claude/CLAUDE.md from /etc/skel automatically
#    (the skel file imports /opt/vps/CLAUDE.md via @-syntax).
adduser --disabled-password --gecos "" "$PROJECT"
usermod -aG docker "$PROJECT"
mkdir -p "/srv/$PROJECT"
chown "$PROJECT:$PROJECT" "/srv/$PROJECT"

# 2. Verify VPS-wide CLAUDE.md was bootstrapped from /etc/skel
test -f "/home/$PROJECT/.claude/CLAUDE.md" \
  && grep -q "^@/opt/vps/CLAUDE.md" "/home/$PROJECT/.claude/CLAUDE.md" \
  && echo "OK: shared manual imported for $PROJECT"
# (If missing — e.g. adduser bypassed skel — copy manually:
#   install -m 644 -o "$PROJECT" -g "$PROJECT" \
#     /etc/skel/.claude/CLAUDE.md "/home/$PROJECT/.claude/CLAUDE.md")

# 3. Allocate a port from /opt/vps/PORTS.md
#    (edit PORTS.md to reserve a row, then paste the port below)
PROJECT_PORT=<reserved port>

# 4. Grant access to shared creds the project needs
#    (group membership is the durable path; ACLs are the per-file
#    override. Use both: group for future expansion, ACL for narrow
#    reads today.)
usermod -aG secrets "$PROJECT"                              # future-proof
setfacl -m u:"$PROJECT":r /etc/secrets/godaddy.creds        # if needed

# 5. Caddy vhost — drop into the shared /opt/vps/vhosts/ location.
#    As the project user, you have ACL write access here (inherited
#    from the default ACL on the dir, granted in Phase 34).
sudo -u "$PROJECT" tee "/opt/vps/vhosts/$PROJECT.caddy" >/dev/null <<EOF
$PROJECT.the-kizz.com {
    encode zstd gzip
    reverse_proxy 127.0.0.1:$PROJECT_PORT {
        flush_interval -1
    }
}
EOF

# 6. Reload Caddy (wildcard cert covers the new subdomain immediately)
docker exec revproxy-caddy-1 caddy reload --config /etc/caddy/Caddyfile

# 7. Project-specific compose up should now bind 127.0.0.1:$PROJECT_PORT
```

## Backups

Nightly (root cron):

```bash
# /etc/cron.daily/backups-projects
for dir in /srv/*; do
  project=$(basename "$dir")
  tar czf "/var/backups/${project}-$(date +%Y%m%d).tgz" "$dir"
done
find /var/backups -name '*.tgz' -mtime +14 -delete
```

Weekly off-site via `restic` or `rclone` to a cheap object store
(B2 / S3 / Wasabi).

## Claude Code operating rules

- `~/.claude/CLAUDE.md` → imports this file via `@/opt/vps/CLAUDE.md`
  (each service user keeps their own thin wrapper; new users get one
  via `/etc/skel/.claude/CLAUDE.md`). Loaded automatically in every
  session.
- Per-project `/srv/<project>/CLAUDE.md` adds project-specific rules
  and is version-controlled.
- `/srv/<project>/CLAUDE.local.md` for per-machine personal overrides
  (gitignored).
- Use `@path/to/file` imports inside any CLAUDE.md to pull in
  additional context (Claude Code supports it natively).

## Scheduled follow-ups (`/opt/vps/FOLLOWUP-*.md`)

Systemd timers (`systemctl list-timers 'vps-followup-*.timer'`) may
drop report files at `/opt/vps/FOLLOWUP-<date>.md` to flag deferred
work. **At session start, check:**

```bash
ls /opt/vps/FOLLOWUP-*.md 2>/dev/null && echo "^ pending follow-up(s)"
```

If one exists, read it, walk through the items with the operator,
then delete the file and disable the matching timer:

```bash
systemctl disable --now vps-followup-<date>.timer
rm /etc/systemd/system/vps-followup-<date>.{service,timer}
systemctl daemon-reload
rm /opt/vps/FOLLOWUP-<date>.md
```

## Known failure modes / gotchas

- **Host bind-mounts silently break** on containers that run as
  non-root when userns-remap is enabled. The container sees permission
  errors on writes. Use **named volumes** (managed by Docker) or
  `tmpfs` for any container-writable path.
- **Docker group = root-equivalent.** Someone in `docker` can mount
  `/` into a container and `chroot`. Don't grant group docker casually;
  treat per-user isolation as a layer, not the wall.
- **Port collisions** on `127.0.0.1:3000-3999` if two projects pick the
  same port. Update `/opt/vps/PORTS.md` when allocating.
- **Fine-grained PATs** have 90-day max expiry. Calendar a rotation
  reminder per project. Classic PATs don't expire and should be
  retired.
